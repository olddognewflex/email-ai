# Hoist type packages and eslint so tools that walk node_modules can find them.
# Avoids shamefully-hoist=true which defeats pnpm's strictness.
auto-install-peers=true
hoist-pattern[]=*types*
hoist-pattern[]=*eslint*
public-hoist-pattern[]=*typescript*
dedupe-direct-deps=true
